# 🧾 Ledger — Expense Tracker

A simple, receipt-styled expense tracker built with vanilla HTML, CSS, and JavaScript. Add expenses, categorize them, and track your running total — all saved locally in your browser.

## 🔗 Live Demo
*(coming soon — will add link after deploying via GitHub Pages)*

## ✨ Features
- Add an expense with a description, category, and amount
- View all expenses in a clean, receipt-style list
- Delete ("void") any expense
- Running total updates automatically
- Data persists across page refreshes using `localStorage`

## 🛠️ Built With
- HTML5
- CSS3 (custom design, no frameworks)
- Vanilla JavaScript (DOM manipulation, localStorage)

## 📸 Preview
*(add a screenshot here once you take one)*

## 🚀 What I Learned
- Working with the DOM to dynamically render UI based on changing data
- Using `localStorage` to persist data without a backend
- Structuring a small app around a single source of truth (the `expenses` array) and re-rendering the UI whenever it changes

## 📂 How to Run
1. Clone or download this repo
2. Open `expense-tracker.html` in any browser
3. Start adding expenses!

## 👩‍💻 Author
**Varsha Patlolla**
[GitHub](https://github.com/patlollavarsha)
